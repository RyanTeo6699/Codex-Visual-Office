{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Prototype-only window capability. Grants only Tauri core defaults and no filesystem, shell, updater, or command execution permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
